<assembly xmlns="http://maven.apache.org/ASSEMBLY/2.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/ASSEMBLY/2.0.0 http://maven.apache.org/xsd/assembly-2.0.0.xsd">
	
	<!--
		This assembly descriptor is used by the build to produce the TGZ deployable for the WebSphere (WAS) server.
		The main build produces a single WAR file, which contains both static and dynamic content, with file serving turned on in ibm-web-ext.xml.
		To run on WebSphere, though, we need a separate WAR (dynamic content) and TGZ (static content), with file serving turned off in ibm-web-ext.xml.
	-->
	
	<id>websphere</id>
	<formats>
		<format>tgz</format>
	</formats>
	<includeBaseDirectory>false</includeBaseDirectory>
	<fileSets>
		<fileSet>
			<directory>${project.basedir}/src/main/webapp</directory>
			<outputDirectory></outputDirectory>
			<excludes>
				<exclude>WEB-INF/**</exclude>
				<exclude>META-INF/**</exclude>
				<exclude>jsp/*.*</exclude>
				<exclude>*.jsp</exclude>
			</excludes>
		</fileSet>
	</fileSets>
</assembly>